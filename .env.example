# OpenRouter API key (recommended method)
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_api_key_here

# Note: If you don't set the environment variable above,
# the system will fall back to reading from config.yaml
# (though environment variable is more secure)

# Optional: Override default model
# OPENROUTER_MODEL=moonshotai/kimi-k2